export interface Object {
  title: string;
  author: string;
  description: string;
  glb: string;
  source: string;
  category: string;
  scale: number;
  date: number;
}

export interface Category {
  title: string;
}